# Copy this file to ".env" and fill in your own cluster string to enable
# persistence. Without a valid MONGODB_URI, ParkSense runs in zero-setup
# in-memory mode (a restart reseeds the demo data).
MONGODB_URI=mongodb+srv://USER:PASSWORD@YOUR-CLUSTER.mongodb.net/?appName=ParkSense
MONGODB_DB=parksense
